<% layout('layouts/boilerplate') %>

    <div class="row">
        <div class="col-6 offset-3">
            <div class="card mb-3">
                <img src="<%= shop.image%>" class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">
                        <%= shop.title%>
                    </h5>
                    <p class="card-text">
                        <%= shop.description%>
                    </p>
                </div>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item text-secondary">
                        <%= shop.location%>
                    </li>
                    <li class="list-group-item">$<%= shop.price%> Avg. (Regular Size, No Toppings)</li>
                </ul>
                <div class="card-body">
                    <a class="card-link btn btn-info" href="/shops/<%=shop._id%>/edit">Edit</a>
                    <form class="d-inline" action="/shops/<%=shop._id%>?_method=DELETE" method="POST">
                        <button class="btn btn-danger">Delete</button>
                    </form>
                </div>
                <div class="card-footer text-muted">
                    2 days ago
                </div>
            </div>

        </div>
        <h2>Leave a Review</h2>
        <form action="/campgrounds/<%=campground._id%>/reviews" method="POST" class="mb-3 validated-form" novalidate>
            <div class="mb-3">
                <label class="form-label" for="rating">Rating</label>
                <input class="form-range" type="range" min="1" max="5" name="review[rating]" id="rating">
            </div>
            <div class="mb-3">
                <label class="form-label" for="body">Review</label>
                <textarea class="form-control" name="review[body]" id="body" cols="30" rows="3" required></textarea>
                <div class="valid-feedback">
                    Looks good!
                </div>
            </div>
            <button class="btn btn-success">Submit</button>
        </form>
        <!-- <h1>
        <%= shop.title %>
    </h1>
    <h2>
        <%= shop.location %>
    </h2>
    <img src="<%= shop.image %> " alt="">
    <p>
        <%= shop.description %>
    </p>
    <p>
        <a href="/shops/<%=shop._id %>/edit">Edit</a>
    </p>
    <p>
    <form action="/shops/<%= shop._id %>?_method=DELETE" method="POST"><button>Delete</button></form>
    </p>
    <footer>
        <a href="/shops">All Shops</a>
    </footer> -->